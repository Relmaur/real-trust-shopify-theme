<div class="divsection-01 hero">
  <div class="section-container full">
    <h1>{{ page.title }}</h1>
    <p>{{ page.metafields.custom.project_info }}</p>
    {% comment %} <img src="{{ page.metafields.custom.project_info.project-0fuyn8pm }}" alt=""> {% endcomment %}
  </div>
</div>

{% schema %}
{
  "name": "Hero",
  "settings": [
    {
        "type": "text",
        "id": "title",
        "label": "Hero Title",
        "default": "Project Hero Section"
    }
  ]
}
{% endschema %}
