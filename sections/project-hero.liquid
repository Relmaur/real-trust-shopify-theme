<div class="divsection-01 hero">
  <div class="section-container full">
    <h1>{{ page.title }}</h1>
    <p>{{ page.metafields.project.project_name }}</p>
  </div>
</div>

{% schema %}
{
  "name": "Hero",
  "settings": [
    {
        "type": "text",
        "id": "title",
        "label": "Hero Title",
        "default": "Project Hero Section"
    }
  ]
}
{% endschema %}
