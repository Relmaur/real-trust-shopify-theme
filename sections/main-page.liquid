<div class="divsection-01 hero">
  <div class="section-container">
    <h1 style="text-align: center;">{% if section.settings.title %} {{ section.settings.title }} {% else %} {{ page.title }} {% endif %}</h1>
  </div>
</div>

{% schema %}
{
  "name": "Hero",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Hero Title",
      "default": "Project Hero Section"
    }
  ]
}
{% endschema %}